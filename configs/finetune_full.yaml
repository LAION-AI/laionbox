# Full LoRA Training Config — 5 epochs on all 8 GPUs
# effective batch = 8 GPUs × 1 × 4 grad_accum = 32

preprocessed_dir: ./finetune_data
output_dir: ./finetune_output/full_run
dramabox_dir: /home/deployer/laion/DramaBox
checkpoint: /home/deployer/laion/DramaBox/models/ltx-2.3-22b-dev-audio-only-v13-merged.safetensors
full_checkpoint: /home/deployer/laion/DramaBox/models/ltx-2.3-22b-dev.safetensors
gemma_root: /home/deployer/.cache/dramabox/models--unsloth--gemma-3-12b-it-bnb-4bit/snapshots/826e729dbaeea4ecb143738eed2bcf3539ebf7bf

base_model: dev
lora_rank: 128
lora_alpha: 128
lora_dropout: 0.05

# Epoch-based training (overrides steps)
epochs: 5
lr: 2.0e-5
lr_scheduler: cosine
batch_size: 1
grad_accum: 4
warmup_steps: 50
max_grad_norm: 1.0
max_ref_tokens: 250
text_dropout: 0.1

save_every: 100
log_every: 10
seed: 42

# Validation
val_samples: 10
val_refs_dir: /home/deployer/laion/test-refs

monitor_port: 8765
